/* Given array of integers, find at every index, mimimum element in the array excluding the element at index i itself. */

import java.io.*;
import java.util.*;

class MinimumIndexArray()
{
	int[] minimumArray(int[] arr)
	{
	
	}
}